==About


==Installing

sudo gem install unicode
sudo gem install hpricot

# clone on github

git remote add rob git://github.com/robmckinnon/ukcompanies.git -t master
git fetch rob master
git submodule init
git submodule update


# update/add your database.yml
rake db:migrate
rake ukcompanies:import_all

Notes: Plugins are submodules..